function customer(customerID,customerName,customerAddress,customerContact){
    return {
        id: customerID,
        name: customerName,
        address: customerAddress,
        contact: customerContact
    }
}